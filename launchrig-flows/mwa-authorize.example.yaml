# Template only: replace the TODO selector before adding this flow to launchrig.yml.
# Purpose: cold authorize and cached reauthorization
appId: dev.launchrig.fixture
---
- launchApp:
    clearState: false
- assertVisible: "TODO: app-ready-selector"
