# LaunchRig pilot notes

Keep this file outside the public evidence JSON. Remove private names, paths, device serials, account details, and unapproved product information before sharing any excerpt.

## Session

- Pilot ID used locally:
- Date:
- Android application ID:
- App build version:
- LaunchRig version:
- Public evidence schema version:
- Approved canonical scope SHA-256:
- Both attended `pilot check` and `pilot run` used the same mandatory `--scope FILE`: yes / no
- Android model and version, without serial:
- Development wallet and version:
- Configured network label:

## Setup

- Timer started at:
- First qualifying run at:
- Measured setup duration:
- Qualifying attempts and total attempts:
- Trailing passes on the current fingerprint:
- Median runtime on the current fingerprint:
- Installation steps that caused friction:
- Selector or accessibility-label changes:
- Documentation gaps:

## Attempts

| Attempt | Outcome | Readiness | Duration | Qualifying | Notes |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |
| 2 |  |  |  |  |  |
| 3 |  |  |  |  |  |

## Publisher feedback

- Most useful part:
- Least clear part:
- Would run before releases:
- Required change before continued use:
- Quote approved for publication:

## Evidence review

- [ ] `launchrig pilot verify` accepted the public JSON.
- [ ] The exact export is scope-linked evidence v3, and its stable scope digest matches the approved session and consent scope digest.
- [ ] The publisher reviewed the relative timing, per-export fingerprint grouping, stable evidence-ID linkability, and stable scope-digest linkability disclosed by evidence v3.
- [ ] The recomputed technical and scope-consistency gates are described as self-recorded and not as consent, publisher, bundle, operator, device, or artifact authentication.
- [ ] Evidence v1 and v2 are treated as historical verification formats that cannot satisfy private scope-linked governance.
- [ ] Public artifacts contain no device serial, secrets, private paths, or unapproved identity.
- [ ] The publisher reviewed every file selected for sharing.
- [ ] Evidence remains labeled self-recorded and unattested.
- [ ] No Seeker, Seed Vault, production-wallet, or confirmed-defect claim is inferred from this pilot.
